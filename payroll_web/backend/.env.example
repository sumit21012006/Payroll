# Server Configuration
PORT=8080
NODE_ENV=development

# Database Connection (Supabase / PostgreSQL)
# Replace with your actual Supabase connection string
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres?schema=public"

# Secure API Token for Biometric Sync Agent (e.g., ADMS connection verification)
SYNC_API_TOKEN="kfil_solapur_secret_sync_token_2026"
